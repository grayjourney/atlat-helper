# Project Management Agent - Requirements
# Python 3.11+ required

# =============================================================================
# Core Framework
# =============================================================================
# FastAPI: Async web framework (like Go's Gin/Echo or PHP's Laravel)
# NOTE: mcp>=1.0 requires starlette>=0.39
# FastAPI 0.115.2+ uses starlette>=0.40.0 which is compatible
fastapi>=0.115.2,<1.0.0

# Uvicorn: ASGI server (like Go's net/http server or PHP-FPM)
uvicorn[standard]>=0.27.0,<0.32.0

# =============================================================================
# Agent Orchestration (LangGraph)
# =============================================================================
langgraph>=0.2.0,<0.4.0
langchain-core>=0.3.0,<0.4.0

# LLM Providers
langchain-google-genai>=2.0.0,<3.0.0
langchain-anthropic>=0.3.0,<0.4.0
langchain-ollama>=0.2.0,<0.3.0

# LangGraph Postgres Checkpointer (for production persistence)
langgraph-checkpoint-postgres>=2.0.0,<3.0.0

# =============================================================================
# Model Context Protocol (MCP)
# =============================================================================
mcp>=1.0.0,<2.0.0
langchain-mcp-adapters>=0.0.1,<0.1.0

# =============================================================================
# Chat UI Framework
# =============================================================================
# Chainlit 2.6.3+ is compatible with FastAPI 0.115.x and starlette 0.40+
chainlit>=2.6.0,<3.0.0

# =============================================================================
# Data Validation & Configuration
# =============================================================================
pydantic>=2.5.0,<3.0.0
pydantic-settings>=2.1.0,<3.0.0
python-dotenv>=1.0.0,<2.0.0

# =============================================================================
# ⚠️  ASYNC-SAFE HTTP & DATABASE (REQUIRED) ⚠️
# =============================================================================
# NEVER use 'requests' or 'psycopg2' - they BLOCK the event loop!

# HTTPX: Async HTTP client (REQUIRED - replaces requests)
# ❌ DO NOT USE: requests (blocks event loop)
# ✅ USE THIS: httpx with AsyncClient
httpx>=0.26.0,<0.29.0

# AsyncPG: Async PostgreSQL driver (REQUIRED for PostgresSaver)
# ❌ DO NOT USE: psycopg2 (blocks event loop)
# ✅ USE THIS: asyncpg
asyncpg>=0.29.0,<0.31.0

# =============================================================================
# Testing
# =============================================================================
pytest>=8.0.0,<9.0.0
pytest-asyncio>=0.23.0,<0.25.0
pytest-cov>=4.1.0,<6.0.0
respx>=0.20.0,<1.0.0

# =============================================================================
# Development Tools
# =============================================================================
mypy>=1.8.0
ruff>=0.2.0
black>=24.1.0
langgraph-checkpoint-sqlite>=1.0.0
